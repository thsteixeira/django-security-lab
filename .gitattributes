# Normalise line endings to LF in the repo so CI (Linux) and contributors on any
# OS see identical bytes (captured scan output and YAML are line-sensitive).
* text=auto eol=lf
*.png binary
*.jpg binary
